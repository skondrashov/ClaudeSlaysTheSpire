# Run Log (deprecated)

Individual run analyses are now stored in `analyst/runs/run_NNN.md`.
Archived summaries are in `analyst/runs/runs_*_summary.md`.

Stats are regenerated by `python regen_stats.py` which parses all run files.
